spring.application.name=LibraryManager

# Note:
# The configuration inside application.properties (or application.yml) varies from project to project.
# You should define properties such as database connection details, Hibernate settings,
# and JPA configurations based on your specific requirements.
#
# Example configuration:
#
## Database Configuration
# spring.datasource.url=jdbc:mysql://localhost:3306/your_database_name
# spring.datasource.username=root
# spring.datasource.password=your_password
#
## JPA & Hibernate Configuration
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
#
# You can modify or add other properties depending on:
# - The database you?re using (MySQL, PostgreSQL, Oracle, etc.)
# - The type of application (microservice, monolith, API-first, etc.)
# - The dependencies added in your pom.xml or build.gradle
#
# Hence, use dependencies and configurations like JPA or Hibernate commands
# as per your project?s setup and needs.
